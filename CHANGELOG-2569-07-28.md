# สรุปการแก้ไข — กฎจาก `ตรวจสอบค่าทดสอบ.docx` (28 ก.ค. 2569)

**ที่มา:** `D:\Desktop\ตรวจสอบค่าทดสอบ.docx` รวมกฎ "จำนวนตัวอย่างต่อหัวข้อทดสอบ" ของพัสดุ 27 รหัส
ซึ่งหลายหัวข้อทดสอบไม่ครบทุกตัวอย่างตามจำนวนสุ่มรวม — ของเดิมเว็บคิดราคาเต็มทุกหัวข้อ จึง**ประมาณการสูงเกินจริง**

แบ่งงานเป็น 3 ส่วน: กฎใหม่ลง `rules.js` · ปิดช่องว่างของ engine ใน `index.html` ·
ข้อมูลผิดที่อยู่ใน Google Sheet (แก้ที่นี่ไม่ได้ เพราะ `data.js` ถูก generate จากชีต) แยกเป็นรายการให้ไปแก้เอง

---

## ไฟล์ที่แก้

| ไฟล์ | แก้อะไร |
|---|---|
| `webapp_sample/index.html` | เพิ่มคีย์กฎ `perTestFixed` ใน `perTestCount()` · ช่อง "จำนวนตัวอย่าง" พิมพ์ทับได้ทุกแถว |
| `webapp_sample/rules.js` | กฎใหม่/แก้ 6 ชุด (ดูข้างล่าง) |
| `cost_estimate (ประมาณการ+ทำ มท)/test_rules.py` | mirror กฎทั้ง 6 ชุด + รองรับ `perTestFixed` · เอา `MANUAL_AGREED` ออก |
| `cost_estimate (ประมาณการ+ทำ มท)/Fill xlsx from gsheet - open.py` | เอา import + บล็อกข้าม `MANUAL_AGREED` ออก |
| `webapp_sample/README.md` | เพิ่มข้อจำกัด 2 ข้อ |
| `webapp_sample/sheet_changes_2569-07-28.md` | **ไฟล์ใหม่** — รายการแก้ Google Sheet 9 รายการ |

---

## 1. กฎที่เพิ่ม/แก้ใน `rules.js` (mirror ลง `test_rules.py` ครบแล้ว)

| รหัสพัสดุ | สเปค | กฎ |
|---|---|---|
| `1020360000` | RCBL-066/2563 | IPC — Visual ทดสอบครบทุกตัวอย่าง, Torque + PF withstand ตามตาราง (สุ่ม 3/5/8 = **ไม่ทดสอบเลย**, 13→4, 20→8, 32→12, 50→20) |
| `1020440008` | RINS-014/2563 | CABLE SPACER — สุ่ม 5 → ไม่ทำลาย 4 / Tensile 1 · สุ่ม 10 → ไม่ทำลาย 8 / Tensile 2 |
| `1030010008/009/010`<br>`1030010104/105/106` | RINS-017/2567<br>RINS-018/2568 | LINE-POST / PIN-POST — Porosity test = **1 ตัวอย่าง** หัวข้ออื่นครบ |
| `1030030000`<br>`1030030100/103` | RINS-020/2568<br>RINS-021/2568 | SPOOL / STRAIN — Dimension = **3**, Porosity test = **1** หัวข้ออื่นครบ |
| `1030020100/101/103` | RINS-003/2561 | **เขียนใหม่ทั้งชุด** — ตัวเลขเดิมไม่ตรงเอกสาร (ดูข้างล่าง) |
| `1040020102` | RPRO-052/2562 | FSD — Glow wire test ตามตาราง (สุ่ม 3/5→1, 8/13/20/32→2, 50→3) หัวข้ออื่นครบ |
| `1040073011/012` | RPRO-014/2559 | VT — Temperature rise = **1 ตัวอย่างเสมอ** ทุกจำนวนส่งทดสอบ |

### RINS-003/2561 — ตัวเลขเดิมผิด

เอกสารแบ่ง "ไม่ทำลาย (Dimension, Leakage distance, Coating thickness) / ทำลาย (Tensile strength)"
ของเดิมใน `rules.js` เอาสองคอลัมน์นี้ไปแปะผิดหัวข้อ

| จำนวนสุ่ม | ของเดิม (ผิด) | แก้เป็น (ตามเอกสาร) |
|---|---|---|
| 3 | *(ไม่มีคีย์ — ใช้ "ตามที่ตกลง")* | Dim/Leak/Coat **2**, Tensile **1** |
| 5 | Dim 5, Leak 5, Tensile 3, Coat 2 | Dim/Leak/Coat **3**, Tensile **2** |
| 7 | Dim 7, Leak 7, Tensile 4, Coat 3 | Dim/Leak/Coat **4**, Tensile **3** |
| 12 | Dim 12, Leak 12, Tensile 8, Coat 4 | Dim/Leak/Coat **8**, Tensile **4** |
| 18 | Dim 18, Leak 18, Tensile 12, Coat 6 | Dim/Leak/Coat **12**, Tensile **6** |

พร้อมกันนี้เอา `manualPerTestBelowQty: 5` ("ตามที่ตกลง") ออก เพราะเอกสารกำหนดชั้น `น้อยกว่า 11 → สุ่ม 3` ไว้แล้ว
→ ฝั่ง `cost_estimate` จึงเลิกข้ามรหัสกลุ่มนี้ กรอกจำนวนรายหัวข้อให้อัตโนมัติได้แล้ว

---

## 2. คีย์กฎใหม่ `perTestFixed`

`perTestFixed: { "<ชื่อหัวข้อ>": จำนวน }` = ทดสอบเท่านี้เสมอ ไม่ว่าจำนวนสุ่มเท่าไร

**ทำไมต้องมี:** `1040073011/012` คิดจำนวนสุ่มเป็น 5% ของจำนวนที่ซื้อ (ขั้นต่ำ 5) จำนวนสุ่มจึงเป็นเลขอะไรก็ได้
คีย์ของ `perTestBySample` เขียนล่วงหน้าไม่ได้

ลำดับการตัดสินใหม่ (เหมือนกันทั้ง `index.html` และ `test_rules.py`):
```
perTestBySample[จำนวนสุ่ม] → perTestFixed → pairTests → fullTests → perTestAlways1 → จำนวนสุ่ม
```

> **หมายเหตุ:** จงใจ**ไม่**ใส่ `"Temperature rise"` และ `"Porosity test"` ลงใน `perTestAlways1` (ลิสต์ที่ใช้กับทุกรหัส)
> เพราะ H.R.C. FUSE `1040020010`–`1040020019` + FSD `1040020102` มีหัวข้อ Temperature rise เหมือนกันแต่ทดสอบครบทุกตัวอย่าง
> และ CABLE SPACER PORCELAIN `1020440006/1020440007` มี Porosity test แต่เอกสารไม่ได้พูดถึง

---

## 3. ช่อง "จำนวนตัวอย่าง" พิมพ์ทับได้ทุกแถว

ของเดิมพิมพ์ได้เฉพาะโหมด "ตามที่ตกลง" ตอนนี้ทุกแถวเป็นช่องกรอก โดยตั้งค่าเริ่มต้นเป็นค่าที่คำนวณให้

- ใส่ `0` = ไม่ทดสอบ (มีป้ายกำกับให้)
- ลบให้ว่าง = กลับไปใช้ค่าที่คำนวณ
- ค่าที่กรอกจะถูกล้างอัตโนมัติเมื่อเปลี่ยน "จำนวนที่ซื้อ" หรือ "จำนวนสุ่ม"
- กรอกเกินจำนวนที่ซื้อไม่ได้ (ใช้ตัวจำกัดเดิม)

ใช้กับกรณีอย่าง `1020440006/1020440007` ที่ชีตระบุจำนวนสุ่ม "ไม่กำหนด" และเอกสารไม่ได้ครอบคลุม — เลือกจำนวน Porosity เองได้

---

## 4. การตรวจสอบที่ทำแล้ว

| ตรวจอะไร | ผล |
|---|---|
| ดึง `computeSamples()` + `perTestCount()` ตัวจริงจาก `index.html` มารันกับ `data.js` | จำนวนรายหัวข้อตรงกับเอกสารทุกกรณี |
| เทียบ `test_rules.py` (Python) กับ `rules.js` (JS) ทุกรหัส × จำนวนสุ่ม 14 ค่า × ทุกหัวข้อ | **13,076 เคส ไม่ต่างกันเลย** (ข้ามกลุ่ม CT/VT ที่ตั้งใจไม่ port เหมือนเดิม) |
| `node --check` ทั้ง `rules.js` และสคริปต์ใน `index.html` | ผ่าน |
| `py_compile` ทั้ง `test_rules.py` และ `Fill xlsx from gsheet - open.py` | ผ่าน |
| ข้อความ "จำนวนสุ่ม" ที่เสนอให้แก้ในชีต ทดสอบกับ `parse_sampling()` ตัวจริง | ผ่านทุกบรรทัด |

ตัวอย่างผลลัพธ์หลังแก้:

| รหัส | ซื้อ | จำนวนสุ่ม | รวม (บาท) |
|---|---|---|---|
| `1020360000` | 10 | 3 | 600 (Torque + PF withstand = ไม่ทดสอบ) |
| `1020360000` | 200 | 13 | 6,600 |
| `1020440008` | 500 | 5 | 34,900 |
| `1030010008` | 100 | 3 | 19,000 (Porosity 1) |
| `1030030100` | 100 | 5 | 8,600 (Dimension 3, Porosity 1) |
| `1030020100` | 1,000 | 7 | 6,700 |
| `1040020102` | 2,000 | 32 | 181,240 (Glow wire 2) |
| `1040073011` | 400 | 20 | 112,600 (Temp rise 1) |

---

## 5. Google Sheet — แก้ครบแล้ว ✅ (28 ก.ค. 2569)

รายการทั้ง 9 รายการใน **`sheet_changes_2569-07-28.md`** แก้ในชีตและรัน `update_data.bat` ครบแล้ว
`data.js` ปัจจุบัน = 280 รายการ (เพิ่ม CROSSARM 2 รหัส) snapshot `28 ก.ค. 2569`

ตรวจผ่านหมดทุกข้อ:

| รหัส | ซื้อ | ผล |
|---|---|---|
| `1020360003` | 35,200 | สุ่ม 11 (เดิม 8) |
| `1030020100` | 20 | สุ่ม 3 · รวม 2,900 (เดิมสุ่ม 5 · 4,800) |
| `1020440008` | 10,000 พอดี | สุ่ม 10 (เดิม 5) |
| `1030030000` | 100 | รวม 8,600 · มี Visual, ไม่มี flashover |
| `1040073011/012` | 400 | 6 หัวข้อครบทั้งคู่ · Temperature rise = 1 |
| `1000120005` | 300 | สุ่ม 15 · 2 หัวข้อ |

### ตัดสินใจแล้ว: RINS-003 ซื้อเกิน 10,000 ตัว → กรอกจำนวนสุ่มเอง

ตารางในเอกสารหยุดที่ชั้น `5,001–10,000` จึงไม่มีกฎรองรับจำนวนที่มากกว่านั้น
**จงใจไม่เพิ่มชั้นบนสุด** ให้หน้าเว็บเด้งไปโหมด "กรอกจำนวนสุ่มเอง" แทน

⚠️ ผลข้างเคียง: ทุกครั้งที่รัน `update_data.bat` จะมี warning 3 บรรทัดแบบนี้เสมอ — **ไม่ใช่บั๊ก ไม่ต้องแก้**
```
1030020100: rule gives no result for qty=50000
1030020101: rule gives no result for qty=50000
1030020103: rule gives no result for qty=50000
```

---

## 6. ข้อสังเกตที่เจอระหว่างทาง

- **`data.js` แก้ด้วยมือไม่ได้** — หัวไฟล์เขียนว่า generate จาก `tools/convert_data.py` การแก้ตรงๆ จะโดนทับเมื่อรัน `update_data.bat`
- **ตัวแปลงจำนวนสุ่มไม่รู้จักคำว่า "น้อยกว่า"** — ต้องเขียนเป็น `1 to N`, `less than N`, หรือ `N ขึ้นไป` ไม่งั้นจะ parse เป็นช่วงผิด
- **บั๊ก 3 ตัวเจอจากการเทียบข้อมูล ไม่ได้อยู่ในเอกสาร** — พิมพ์ผิด 35,500 ที่ทับชั้นถัดไป, ขอบ 10,000 ของ `1020440008`, และ `1040073011` กับ `1040073012` เป็นสเปคเดียวกันแต่หัวข้อในชีตไม่ตรงกันเอง
- **`manualPerTestBelowQty` ไม่มีรหัสไหนใช้แล้ว** แต่ยังเก็บโค้ดกับคำอธิบายไว้เป็นคีย์สำรอง
