# รายการแก้ Google Sheet — จาก `ตรวจสอบค่าทดสอบ.docx` (28 ก.ค. 2569)

> ## ✅ แก้ครบทั้ง 9 รายการแล้ว (28 ก.ค. 2569) — เก็บไฟล์นี้ไว้อ้างอิงเฉยๆ
> รัน `update_data.bat` แล้ว `data.js` = 280 รายการ ตรวจผ่านทุกข้อตามหัวข้อ "ตรวจหลังแก้" ท้ายไฟล์
>
> **หมายเหตุ RINS-003 (`1030020100/101/103`):** ตารางหยุดที่ `5,001–10,000` ซื้อเกิน 10,000 ตัว
> ตัดสินใจแล้วว่า**ให้กรอกจำนวนสุ่มเอง** ไม่เพิ่มชั้นบนสุด → `update_data.bat` จะเตือน
> `rule gives no result for qty=50000` 3 บรรทัดทุกครั้ง **เป็นเรื่องปกติ ไม่ใช่บั๊ก**

`data.js` ถูก generate จากชีตด้วย `tools/convert_data.py` **แก้ด้วยมือไม่ได้**
(ชีต `13-O-DYPJ_T98LJRIByvGRT82gF1lWfpizEcOzvwaOR4`)

ขั้นตอน: แก้ในชีตตามตารางข้างล่าง → รัน `webapp_sample\update_data.bat` → ตรวจตามหัวข้อ "ตรวจหลังแก้"

> ข้อความในคอลัมน์ **"แก้เป็น"** ผ่านการทดสอบกับ `parse_sampling()` ตัวจริงแล้วทุกบรรทัด
> ⚠️ ตัวแปลงไม่รู้จักคำว่า **"น้อยกว่า"** — ถ้าจะเขียนช่วงล่างต้องใช้ `1 to N`, `less than N`, หรือ `N ขึ้นไป` เท่านั้น

---

## A. คอลัมน์ "จำนวนสุ่ม"

### A1 · `1020360003`, `1020360004`, `1020360005`, `1020360006`, `1020360007` — IPC (RCBL-066/2567)

พิมพ์ผิด `35,500` ทำให้ทับกับชั้น `35,001 ขึ้นไป` → **ซื้อ 35,001–35,500 ตัวได้สุ่ม 8 แทนที่จะเป็น 11**

| | |
|---|---|
| ตอนนี้ | `501  to  35,500  : 8` |
| แก้เป็น | `501  to  35,000  : 8` |

(บรรทัดอื่นในเซลล์เดิมไม่ต้องแตะ)

### A2 · `1030020100`, `1030020101`, `1030020103` — COMPOSITE SUSPENSION INSULATOR (RINS-003/2561)

เอกสารมีชั้นล่าง `น้อยกว่า 11 → 3` และ `11–30 → 3` ที่ชีตยังไม่มี → **ซื้อ 4–30 ตัวได้สุ่ม 5 แทนที่จะเป็น 3**
(ซื้อ 1–3 ตัวได้คำตอบถูกอยู่แล้ว เพราะหน้าเว็บปรับจำนวนสุ่มลงเท่าจำนวนที่ซื้อ)

| | |
|---|---|
| ตอนนี้ | `N ≤ 300   : 5`<br>` 300 < N ≤ 2000 : 7`<br>` 2000 < N ≤ 5000  : 12`<br>` 5000 < N ≤ 10000  : 18` |
| แก้เป็น | `1 to 30 : 3`<br>`31 to 300 : 5`<br>`301 to 2,000 : 7`<br>`2,001 to 5,000 : 12`<br>`5,001 to 10,000 : 18` |

(เอกสารแยก `<11` กับ `11–30` เป็นสองชั้นแต่สุ่มเท่ากันคือ 3 จึงรวมเป็นชั้นเดียวได้)

### A3 · `1020440008` — CABLE SPACER (RINS-014/2563)

เอกสารบอก **"ตั้งแต่ 10000"** → 10 แต่ตอนนี้ **ซื้อ 10,000 พอดีได้สุ่ม 5**
(ตัวแปลงอ่าน `<10,000` เป็น 1–9,999 แล้วยืดขอบบนไปปิดช่องว่างที่ 10,000 ให้เอง)

| | |
|---|---|
| ตอนนี้ | `<10,000 : 5`<br>`>10,000 : 10` |
| แก้เป็น | `1 to 9,999 : 5`<br>`10,000 and more : 10` |

---

## B. คอลัมน์ "หัวข้อทดสอบ"

ราคาในวงเล็บลอกมาจากหัวข้อชื่อเดียวกันของรหัสอื่นที่มีอยู่ในชีตแล้ว — **ทานกับอัตราค่าบริการฉบับปัจจุบันก่อนกรอก**

### B1 · `1030030000` — INSULATOR, SPOOL (RINS-020/2568)

| ทำอะไร | หัวข้อ |
|---|---|
| ➕ เพิ่ม | `Visual test (200)` |
| ➖ ลบ | `Power frequency dry flashover test (3500)` |

พร้อมกันนี้: คอลัมน์ **"ที่มาของจำนวนสุ่ม"** ของรหัสนี้ว่างอยู่ → ใส่ `สเปค` (รหัสอื่นในกลุ่มเดียวกันใส่ไว้หมดแล้ว)

### B2 · `1040070009`, `1040070107` — LB SW., SF6 (RPRO-014/2559)

| ทำอะไร | หัวข้อ |
|---|---|
| ➕ เพิ่ม | `Power frequency dry withstand test up to 5 kV (500)` |
| ➕ เพิ่ม | `Power frequency dry withstand test up to 400 kV (2200)` |

### B3 · `1040070305` — CONTROL UNIT, AUTO (RPRO-014/2559)

เอกสารระบุแค่ 2 หัวข้อ แต่ชีตมี 3 หัวข้อที่ไม่ตรงเลย

| ทำอะไร | หัวข้อ |
|---|---|
| ➕ เพิ่ม | `Power frequency dry withstand test up to 5 kV (500)` |
| ➖ ลบ | `Accuracy test (2100)` |
| ➖ ลบ | `Operation test with additional equipment (3400)` |
| ✔ คงไว้ | `Function test (3800)` |

### B4 · `1040073011` — VT.3P.O/D 22,000/110-220 V. (RPRO-014/2559)

ตอนนี้มีแค่ 2 หัวข้อ (Internal PD + Temperature rise) เอกสารระบุ 6 หัวข้อ

| ทำอะไร | หัวข้อ |
|---|---|
| ➕ เพิ่ม | `Power frequency dry withstand test up to 5 kV (500)` |
| ➕ เพิ่ม | `Power frequency dry withstand test up to 400 kV (2200)` |
| ➕ เพิ่ม | `Resistance, resistivity, conductivity (650)` |
| ➕ เพิ่ม | `Accuracy test (2100)` |

### B5 · `1040073012` — VT.3P.O/D 33,000/110-220 V. (RPRO-014/2559)

รหัสนี้กับ `1040073011` เป็นสเปคเดียวกันแต่หัวข้อในชีตไม่ตรงกันเอง — เอกสารบอกว่าต้องเป็น 400 kV ทั้งคู่

| ทำอะไร | หัวข้อ |
|---|---|
| ✏️ แก้ | `Power frequency dry withstand test up to 50 kV (1200)` → `Power frequency dry withstand test up to 400 kV (2200)` |
| ➕ เพิ่ม | `Resistance, resistivity, conductivity (650)` |

---

## C. เพิ่มแถวใหม่ (ยังไม่มีในระบบเลย — เอกสารเขียนว่า "เพิ่มรหัสพัสดุ")

| คอลัมน์ | `1000120005` | `1000120007` |
|---|---|---|
| รหัสพัสดุ | `1000120005` | `1000120007` |
| รายการพัสดุ | `CROSSARM ST. 100X100X3.2 MM. 2.5 M.LONG` | `CROSSARM ST. 100X100X3.2 MM. 3 M.LONG` |
| เลขที่สเปค | `RPRO-014/2559` | `RPRO-014/2559` |
| จำนวนสุ่ม | `not less than 5%`<br>`but not less than 5 set` | (เหมือนกัน) |
| ที่มาของจำนวนสุ่ม | `สเปค` | `สเปค` |
| หัวข้อทดสอบ | `Dimension up to 30 points (400)`<br>`Coating thickness (Magnetic, Eddy current) (400)` | (เหมือนกัน) |
| สถาบันทดสอบ | `กองทดสอบอุปกรณ์ไฟฟ้า (กทฟ.)` | (เหมือนกัน) |
| URL สเปค | (ถ้ามี) | (ถ้ามี) |

---

## ไม่ต้องแก้ (ตรวจแล้ว)

- `1020360003`–`007` ชั้นแรกในชีตเริ่มที่ 1 แต่เอกสารเริ่มที่ 5 / `1020360000` กับ `1040020102` เริ่มที่ 1 แต่เอกสารเริ่มที่ 3
  — หน้าเว็บปรับจำนวนสุ่มลงเท่าจำนวนที่ซื้ออยู่แล้ว ล็อตเล็กจึงได้คำตอบถูกต้อง

---

## ตรวจหลังแก้

รัน `webapp_sample\update_data.bat` แล้วเปิด `index.html` ตรวจ:

| รหัส | ซื้อ | ต้องได้ |
|---|---|---|
| `1020360003` | 35,200 | สุ่ม **11** (เดิมได้ 8) |
| `1030020100` | 20 | สุ่ม **3** → Dimension/Leakage/Coating 2, Tensile 1 (เดิมได้สุ่ม 5) |
| `1030020100` | 1,000 | สุ่ม 7 → ไม่ทำลาย 4 / Tensile 3 · รวม 6,700 บาท |
| `1020440008` | 10,000 | สุ่ม **10** → ไม่ทำลาย 8 / Tensile 2 (เดิมได้สุ่ม 5) |
| `1030030000` | 100 | มี Visual test, ไม่มี Power frequency dry flashover, Dimension 3, Porosity 1 |
| `1040073011` | 400 | 6 หัวข้อ · Temperature rise = **1** ตัวอย่าง (มีดาว ★) |
| `1000120005` | — | ค้นหาเจอ, สุ่ม 5% ขั้นต่ำ 5, มี 2 หัวข้อ |
